git kullanımı 
git  checkout //branches// değiştirme yapılıyor
git  yeni bir dal oluşturur ve o dala geçer
test.cs dosyasında degiştklik yapıldı

git add --all 
git add  test.cs değişiklik eklendi


//devi masterla bileştirmek için önce naster a geç

git checkout master
git merge dev


git status projenin durumu gösterir hangi dosya paketlenecek gösterilir
git log yapılan verisyonları listeler // nu pushmaralar var versiyonların yanında 
git checkout //versiyon numarası// numarası verilen versiyona dönüş sağlanır

cakışma olması durumu 
dosya içindeki değişiklikleri biliyorsa düzenleybilirsin yoksa projenin ilgili kişisiyle iletişime geçilir

dosya içinde  dosyada çakışan yerler head ve ===  ile gösterilir 


git clone //bağlantı// uzak sunucudan dosyayı kopyamak için 

indirilen dosya içerisinde git bash here


değişiklik yap versiyonla 

uzak sunucuya 
git push origin main  ile gönder


git fetch uzak sunucudaki değişiklikler belirler
git pull git üzerindeki dosyayı alır
 originlog
masaüstünde dal açılınca 
git push origin //dalın adı// uzak masaüstüne dalı gönderdik

dalları uzak sunucuda birleştirmek
pull request ile izin gönderiyoruz

puldan sonra çıkan isimlendirme hatası için çıkış
i 'esc'
:wq

git revert
git deff
git reset 
git stash gecici olarak kaydetme commitleme yapmadan  dallar arasında geçiş sağlar
